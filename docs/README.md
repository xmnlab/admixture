# Documentation

This directory contains the Quarto documentation website for `admixture`.

Render locally:

```bash
conda activate admixture
quarto render docs
```

Preview locally:

```bash
quarto preview docs
```

Generated output is written to `docs/_site/` and should not be committed.
